# ── 에이전트 / LLM ──
langgraph
langchain
langchain-aws            # ChatBedrockConverse
boto3


# ── 서빙 / UI ──
fastapi
uvicorn[standard]
chainlit
httpx                    # ui.py SSE 클라이언트

# ── DB / 검색 ──
psycopg2-binary
sqlalchemy
pgvector
rank-bm25                # 키워드 보조(또는 tsvector)

# ── 임베딩 ──
sentence-transformers
transformers
torch
numpy

# ── 계약 / 검증 ──
pydantic>=2
python-dotenv

# ── 데이터 수집 / 정제 ──
requests
lxml

# ── 개발 ──
pytest
ruff